import styled from "styled-components";

export const StyledMain = styled.main`
  background-color: var(--primary-background-color);
  max-width: var(--max-width);
  padding: var(--main-padding-full);
  margin: auto;
`;
